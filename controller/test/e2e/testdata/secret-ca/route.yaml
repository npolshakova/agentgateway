apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: secret-ca-route
  namespace: agentgateway-base
spec:
  parentRefs:
  - name: gateway
  hostnames:
  - secret-ca.example.com
  rules:
  - backendRefs:
    - name: backend
      port: 443
