_err: "min' value must be less than or equal to the 'max"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: shutdown-min-gt-max
spec:
  shutdown:
    min: 61
    max: 60
---
# min and max are required, which avoids the more confusing error
# "AgentgatewayParameters/shutdown-min-gt-max/: spec.shutdown: Invalid value:
# \"object\": no such key: min evaluating rule: The 'min' value
# must be less than or equal to the 'max' value."
_err: "AgentgatewayParameters/shutdown-min-gt-max/: spec.shutdown.min: Required value"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: shutdown-min-gt-max
spec:
  shutdown:
    max: 60
---
_err: "AgentgatewayParameters/shutdown-min-gt-max/: spec.shutdown.max: Required value"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: shutdown-min-gt-max
spec:
  shutdown:
    min: 0
---
_err: "Unsupported value"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: logging-format-invalid
spec:
  logging:
    format: Unknown
---
_err: "Unsupported value"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: workload-kind-invalid
spec:
  workload:
    kind: StatefulSet
---
_err: "deployment overlays are only valid when workload.kind is Deployment or unset"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: daemonset-with-deployment-overlay
spec:
  workload:
    kind: DaemonSet
  deployment:
    spec:
      replicas: 3
---
_err: "daemonSet overlays are only valid when workload.kind is DaemonSet"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: deployment-with-daemonset-overlay
spec:
  daemonSet:
    spec:
      updateStrategy:
        type: RollingUpdate
---
_err: "horizontalPodAutoscaler is not valid when workload.kind is DaemonSet"
apiVersion: agentgateway.dev/v1alpha1
kind: AgentgatewayParameters
metadata:
  name: daemonset-with-hpa-overlay
spec:
  workload:
    kind: DaemonSet
  horizontalPodAutoscaler:
    spec:
      minReplicas: 1
      maxReplicas: 3
